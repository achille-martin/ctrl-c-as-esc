# Ctrl+C As Escape

Neovim plugin to make `Ctrl+c` behave as `Escape`.

## Motivation

If you are a huge fan of terminals, or if you are a lover of handy key maps, then this plugin is likely for you.

This plugin is probably not "philosophically" correct, but it is convenient.
